# Public URL shown in the browser (Next.js rewrites proxy /backend/* to API_URL)
NEXT_PUBLIC_API_URL=/backend

# Server-side backend target for Next.js rewrites (not exposed to browser)
# Local development:
API_URL=http://localhost:8001
# Docker Compose (set via compose build args / environment):
# API_URL=http://gateway:80
